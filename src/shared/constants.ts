export const TILE = Object.freeze({
   ROWS: 6,
   COLUMNS: 10,
})

export const UNITSTATS = Object.freeze({
   hp: 50,
   dmg: 10,
})

export const MSG_TYPES = Object.freeze({
   ID: "socketID",
   SUCCESS: "success",
   JOIN_GAME: "join_game",
   LEAVE_GAME: "leave_game",
})
